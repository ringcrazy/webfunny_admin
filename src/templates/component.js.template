import "./index.scss"
import React, { Component } from "react"

class {0} extends Component {
  constructor(props) {
    super(props)
  }

  componentDidMount() {
  }

  render() {
    return <div className="{1}-container">{1}</div>
  }
}

{0}.propTypes = {
}

export default {0}
